"""Python reference implementation of the Velocitron 0.1.0 contracts.

The package provides net models and parsing/validation, handler contracts and
registries, the firing engine and journal hooks, composition, CEL adapters,
static lint and property analysis, runtime orchestration, and sqlite-backed
durability with handler-free replay. Public classes and functions live in
their respective submodules; the package root does not re-export them.
"""
